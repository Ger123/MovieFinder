<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="commaterialdesign.horgan.gerard.myapplication" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="commaterialdesign.horgan.gerard.myapplication.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="commaterialdesign.horgan.gerard.myapplication.activities.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="commaterialdesign.horgan.gerard.myapplication.activities.SubActivity"
            android:label="@string/title_activity_sub" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="commaterialdesign.horgan.gerard.myapplication.activities.MainActivity" />
        </activity>
        <activity
            android:name="commaterialdesign.horgan.gerard.myapplication.activities.ActivityWithSlidingTabLayout"
            android:label="@string/title_activity_activity_using_tab_library" >
        </activity>

    </application>

</manifest>